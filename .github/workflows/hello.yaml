name: Hello World CI

# Run on any push or pull request
on:
  push:
    branches:
      - "**"       # triggers on all branches
  pull_request:
    branches:
      - "**"       # triggers on PRs to any branch

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "🎉 Hello from GitHub Actions on branch $GITHUB_REF!"
